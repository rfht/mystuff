$OpenBSD$

debug printfs

Index: mono/sgen/sgen-thread-pool.c
--- mono/sgen/sgen-thread-pool.c.orig
+++ mono/sgen/sgen-thread-pool.c
@@ -127,6 +127,7 @@ has_priority_work (int worker_index, int current_conte
 static void
 get_work (int worker_index, int *work_context, int *do_idle, SgenThreadPoolJob **job)
 {
+	printf("get_work (sgen-thread-pool.c), thread: %p\n", pthread_self());
 	while (!threadpool_shutdown) {
 		int i;
 
@@ -169,6 +170,7 @@ get_work (int worker_index, int *work_context, int *do
 static mono_native_thread_return_t
 thread_func (void *data)
 {
+	printf("thread_func (sgen-thread-pool.c), thread: %p\n", pthread_self());
 	int worker_index = (int)(gsize)data;
 	int current_context;
 	void *thread_data = NULL;
