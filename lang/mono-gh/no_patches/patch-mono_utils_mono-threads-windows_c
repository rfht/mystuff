$OpenBSD$

DEBUG printf

Index: mono/utils/mono-threads-windows.c
--- mono/utils/mono-threads-windows.c.orig
+++ mono/utils/mono-threads-windows.c
@@ -53,6 +53,7 @@ mono_threads_suspend_begin_async_suspend (MonoThreadIn
 	}
 
 	/* We're in the middle of a self-suspend, resume and register */
+	printf("calling mono_threads_transition_finish_async_suspend from mono_threads_suspend_begin_async_suspend (mono-threads-windows.c)\n");
 	if (!mono_threads_transition_finish_async_suspend (info)) {
 		mono_threads_add_to_pending_operation_set (info);
 		result = ResumeThread (handle);
