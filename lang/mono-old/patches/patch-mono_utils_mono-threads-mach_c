$OpenBSD$

DEBUG printf

Index: mono/utils/mono-threads-mach.c
--- mono/utils/mono-threads-mach.c.orig
+++ mono/utils/mono-threads-mach.c
@@ -76,6 +76,7 @@ mono_threads_suspend_begin_async_suspend (MonoThreadIn
 		return FALSE;
 
 	/* We're in the middle of a self-suspend, resume and register */
+	printf("calling mono_threads_transition_finish_async_suspend from mono_threads_suspend_begin_async_suspend (mono/utils/mono-threads-mach.c)\n");
 	if (!mono_threads_transition_finish_async_suspend (info)) {
 		mono_threads_add_to_pending_operation_set (info);
 		do {
