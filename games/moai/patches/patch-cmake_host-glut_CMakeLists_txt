$OpenBSD$

remove rt
fix name of libglut

Index: cmake/host-glut/CMakeLists.txt
--- cmake/host-glut/CMakeLists.txt.orig
+++ cmake/host-glut/CMakeLists.txt
@@ -55,8 +55,7 @@ endif (BUILD_WINDOWS)
 if ( BUILD_LINUX )
   target_link_libraries ( moai 
     pthread 
-    rt
-    freeglutstatic
+    glut
     Xi
   )
 endif ( BUILD_LINUX )
